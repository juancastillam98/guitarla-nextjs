/*
"use client";
import {createContext, useState} from "react"

export const funcionContext = createContext(null);//estructura para crear el context
//crear cunción ustilizando el props name
export default function CarritoContext({ children }) {


    return (
        <funcionContext.Provider value={{ carrito, setCarrito, agregarCarrito, eliminarProducto, actualizarCantidad }}>
            {children}
        </funcionContext.Provider>
    );
}
*/

